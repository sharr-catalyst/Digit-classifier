# Digit-classifier
A TensorFlow digit classifier using Kaggle's MNIST dataset. The neural network has 128 neurons with dropout regularization, achieving ~97-98% accuracy in 5 epochs. Includes training visualization and model saving for testing on new images.
